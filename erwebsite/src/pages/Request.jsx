import { ERRequestHandler } from "../components/ERRequestHandler";

// page component that handles reimbursement request
export const Request = () => {
    return(
        <ERRequestHandler/>
    );
}